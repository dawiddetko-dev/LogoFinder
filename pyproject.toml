[project]
name = "logo-verification-poc"
version = "0.1.0"
description = "POC for trademark/logo verification with Azure AI"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
