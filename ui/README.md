# UI usage

Run from project root:

```bash
bash launch_ui.sh
```

Or manually:

```bash
PYTHONPATH=src streamlit run ui/app.py
```

UI supports:
- single image upload and detection
- batch verification for files in `data/products_to_verify/`
